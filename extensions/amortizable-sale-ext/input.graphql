query Input {
  cart {
    cost
    lines {
      quantity
      merchandise {
        ...on ProductVariant {
          id
        }
      }
    }
  }
  discountNode {
    metafield(namespace: "discounts-plus", key: "volume-config") {
      value
    }
  }
  presentmentCurrencyRate
}